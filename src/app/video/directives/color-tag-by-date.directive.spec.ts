import { ColorTagByDateDirective } from './color-tag-by-date.directive';

describe('ColorTagByDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTagByDateDirective();
    expect(directive).toBeTruthy();
  });
});
